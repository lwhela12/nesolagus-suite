[phases.setup]
nixPkgs = ["nodejs-18_x", "npm-9_x"]

[phases.install]
cmds = ["npm ci --include=dev"]

[phases.build]
cmds = [
  "ls -la node_modules/.bin/",
  "npm run build"
]

[start]
cmd = "npm start"